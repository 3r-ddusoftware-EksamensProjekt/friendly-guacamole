<!DOCTYPE html>
<html>
    <body>
        <div class="container">
            <h2>Database Results</h2>

            <ul>
                <% results.forEach(function(row) { %>
                <li><%= row.dept_no %>: <%= row.dept_name %></li>
                <% }); %>
            </ul>
        </div>
    </body>
</html>
